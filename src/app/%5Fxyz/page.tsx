// If you want to see this file in Browser change folder _ with %5F

const privateFolder = () => {
  return (
    <div>This is a file inside a private folder, you can't access in browser</div>
  )
}

export default privateFolder;